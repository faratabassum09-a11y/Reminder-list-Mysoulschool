MONGO_URI=mongodb://127.0.0.1:27017/reminder_list
PORT=5000
# Comma-separated list of allowed frontend origins in production, e.g.
# https://your-app.vercel.app. Leave unset for local dev (allows all).
FRONTEND_URL=
