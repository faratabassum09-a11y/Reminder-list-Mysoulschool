# In local dev, leave this unset — Vite proxies /api to http://localhost:5000.
# For production (Vercel), set this to your deployed Render backend's URL:
VITE_API_URL=https://your-backend.onrender.com/api
